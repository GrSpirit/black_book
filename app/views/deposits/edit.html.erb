<h1>Editing Deposit</h1>

<%= react_component "DepositForm", {deposit: @deposit, edit: true, post_url: deposit_path(@deposit), back_url: deposits_path}%>
<%= render 'form', deposit: @deposit %>

<%= link_to 'Show', @deposit %> |
<%= link_to 'Back', deposits_path %>
